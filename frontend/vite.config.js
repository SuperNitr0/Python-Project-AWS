// frontend/vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  root: './', // Ensure this points to the correct directory
  build: {
    outDir: 'dist', // You can change this to the desired output directory
  },
});
